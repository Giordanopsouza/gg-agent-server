# Frontend

React/Vite client for the Task API. Keep API calls in `src/api.ts` and route
them to the backend over HTTP. The frontend does not import the Python SDK.
Run `npm test` and `npm run build` after changes.
